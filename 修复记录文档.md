# 修复记录文档

作者：肖沐樑　QQ：3387432690
文档建立时间：2026，09，18

本文件记录本项目每一次功能修改 / 新增文件的来龙去脉，便于追溯与软著登记。

---

## 2026，09，18　将项目同步推送至 GitHub 仓库

### 背景
赛题要求代码托管到代码平台。此前已推送 Gitee，用户追加要求同步到 GitHub 仓库 `LIHUA5645/15-`。

### 操作
- 目标仓库 `LIHUA5645/15-` 已存在（公开，`main` 分支原有 1 个初始化提交）；
- 采用「个人访问令牌（PAT，classic，scope = repo）」方式经 HTTPS 认证；
- 新增远程别名 `github`（`https://github.com/LIHUA5645/15-.git`，纯地址、不含令牌）；
- 执行强推 `git push github master:main --force`，用本项目覆盖原初始化提交。

### 结果
- GitHub `main` 分支现指向最新提交 `ae8313d`（与本地、Gitee 一致）；
- 令牌仅用于当次命令，未写入 remote 配置、未存入凭据管理器；
- 推送过程未改动任何业务代码与本地 Git 配置。

---

## 2026，09，18　补齐开源交付文件（README / LICENSE）并初始化 Git 仓库推送 Gitee

### 背景
赛题「开源工程规范」要求仓库具备 README、LICENSE、.gitignore，且代码需托管到代码平台。
此前项目只有 `.gitignore`，缺 README 与 LICENSE，且目录尚未初始化为 Git 仓库。
本次按用户指令补上这两个文件，并把整个项目推送到 Gitee。

### 本次新增文件
- `README.md`：完整项目说明——简介 / 功能特性 / 技术栈 / 目录结构 / 三种运行方式（浏览器、Docker、桌面包）/ API Key 配置与脱敏 / 一键构建 / CI / 许可证 / 作者。
- `LICENSE`：MIT 开源许可证，版权人「肖沐樑」。

### 本次改动说明
- 未修改任何业务代码，仅新增开源交付文件；
- Git 推送沿用系统已配置好的全局 `user.name` / `user.email`，未改动任何 Git 配置；
- 仓库新增 `.gitignore` 已排除 `__pycache__/`、`data/`、`release/`、`.env` 等，确保密钥与产物不入库；
- 修正 `README.md` 中 Gitee 克隆地址的账号拼写（由 `zhang-san-zhanshan` 更正为 `zhang-san-zhangshan`，以实际仓库 `git ls-remote` 探测结果为准）；
- 已在项目目录执行 `git init` 并推送到 `https://gitee.com/zhang-san-zhangshan/15-minute-living-circle.git`。

---

## 2026，09，18　从零搭建「15 分钟生活圈智能体检助手」完整程序

### 背景
赛题要求基于百度地图开放能力，开发一款能计算「15 分钟步行等时圈」、统计圈内民生设施覆盖、输出可视化体检报告、标注服务盲区的应用，并要求开源源码、一键构建脚本、技术文档、CI/CD。
此前已有《方法.md》（编译打包方法）与《设计思路.md》（产品设计思路），本次按用户指令把整套程序真正落地实现。

### 本次新增文件清单

#### 后端（Python / FastAPI）
- `requirements.txt`：Python 依赖（fastapi / uvicorn / httpx / pydantic / python-dotenv）。
- `app/__init__.py`：算法包说明。
- `app/config.py`：AK 读取优先级（用户配置 > 环境变量 > .env）、日志脱敏 `yinshen`、降级判定 `shifou_jiangji`。
- `app/cache.py`：SQLite 缓存 `huancun.db`（运行时生成，不入库），指纹 `shengcheng_biaoshi` + 读写 `duqu_huancun` / `xieru_huancun`。
- `app/map_api.py`：百度地图封装——地理编码 `zuobiao_bianma`、POI 周边检索 `huoqu_poi`、批量距离矩阵 `jisuan_juzhen`、直线距离兜底 `zhixian_juli` / `buxing_shichang`。
- `app/isochrone.py`：等时圈 `jisuan_dengshiquan`，扇形采样 + 批量测时 + 时间场候选点。
- `app/blindspot.py`：盲区识别 `shibie_mangqu`，方位抽样判缺失设施。
- `app/score.py`：覆盖度评分 `jisuan_pingfen`，按数量达标 + 最近可达时长打 1~5 星。
- `server.py`：FastAPI 入口，路由 `/api/health`、`/api/config`、`/api/tijian`，并托管 `web/` 静态资源（打包态走 `_MEIPASS`）。

#### 前端（纯静态，由后端托管）
- `web/index.html`：页面骨架（顶栏 / 地图 / 报告区），主流程编排内联。
- `web/css/style.css`：样式。
- `web/js/api.js`：接口封装 `huoquPeizhi` / `tijian`。
- `web/js/map.js`：百度地图封装 `zairuBaidu` / `chushihuaDitu` / `huatReheat` / `huaBiaoji`。
- `web/js/chart.js`：ECharts 柱状图 `huatZhuzhuang`。
- `web/js/score.js`：星级进度条 `xianshiPingfen` + 盲区清单 `xianshiMangqu`。

#### Electron 桌面壳
- `main.js`：主进程 `zhunbeiShujuMulu` / `xuanzeHouduan` / `qidongHouduan` / `dengDaiJiankang` / `guanbiJinchengShu`。
- `preload.js`：安全桥（保留隔离结构）。
- `package.json`：含 `build` 字段（asar + asarUnpack server.exe，nsis + zip 双目标）。

#### 交付物
- `scripts/build.bat`：一键打包（图标→后端→Electron）。
- `scripts/make-icon.ps1`：图标生成。
- `Dockerfile` / `docker-compose.yml`：评委一键起 Web 版。
- `.gitee/workflows/build.yml`：CI 门禁（flake8 + pytest + 自动打包）。
- `.env.example`：密钥样例；`.gitignore`：忽略密钥 / 产物 / 缓存。

### 关键实现说明
1. **拼音变量名**：按软著规范，自定义业务标识符全部用拼音（如 `jisuan_dengshiquan`、`shibie_mangqu`、`huoqu_ak`）；百度 API 参数、BMap 等平台保留名沿用英文。
2. **无 AK 降级**：未配 `BAIDU_AK` 时后端自动用直线距离 ÷ 步行速度估算，等时圈 / 评分 / 盲区仍可演示；前端未配 `WEB_AK` 时底图不渲染但图表照常。保证评审无密钥也能跑通全流程。
3. **密钥脱敏**：`.env` / `peizhi.json` 不入库；日志里 AK 走 `yinshen` 只留头尾 3 位。
4. **缓存**：相同中心点 + 半径 + 设施组合命中 SQLite 缓存，二次秒出，避免重复烧额度。
5. **退出清理**：`main.js` 用 `taskkill /F /T /PID` 结束整棵进程树，防止后端孤儿进程占端口。

### 待办 / 遗留
- 样例城市与社区待定；设施「宜居基线」阈值在 `score.py` 的 `JIXIAN` 中可调整。
- `web/vendor/echarts.min.js` 需用户自行放入（或联网自动走 CDN 兜底）。
- `assets/logo.png` 需提供图标源图，再跑 `build.bat` 生成 `logo.ico`。
- 报告导出（PDF / 图片）尚未实现，见《设计思路.md》第十二节。
