# 15 分钟生活圈智能体检助手 · 编译与打包方法

作者：肖沐樑　QQ：3387432690
完成时间：2026，09，18

本文记录本赛题作品从源码**编译、打包、部署**出可交付成品的完整步骤，
换一台新电脑（或评审老师拿到仓库）也能照着从零跑通。

整体是「一套后端 + 两种形态」：

```text
Web 形态：  浏览器  ──→  Python 后端（自带静态页面 + 地图 API 代理 + 算法）
桌面形态：  Electron 壳 ──→ 拉起同一个 Python 后端 ──→ 本地窗口
```

对应的三段编译：

```text
后端 (server.py)      →  PyInstaller       →  server.exe（用户机器免装 Python）
Electron 壳 (main.js) →  electron-builder  →  安装包 / 绿色版 zip
图标 (logo.png)       →  png-to-ico        →  logo.ico（窗口 / 任务栏 / exe 图标）
```

之所以让 Electron 壳只做壳、所有业务逻辑都放在 Python 后端里，
是为了让**两种形态共用同一份代码**：评委用浏览器（或 Docker）就能验证全部功能，
桌面版只是多包了一层皮，也省得 Web 界面改一次要同步两处。

---

## 零、从零开始（换一台新电脑第一次跑通）

1. 装 **Node.js 18+**（建议 LTS），确认 `node -v`、`npm -v` 都能出来；
2. 装 **Python 3.10+**（安装时勾选 Add to PATH），确认 `py --version`；
3. 拉代码：
   ```bash
   git clone https://gitee.com/你的账号/life-circle-check.git
   cd life-circle-check
   ```
4. 建虚拟环境并安装 Python 依赖：
   ```powershell
   py -m venv .venv
   .\.venv\Scripts\python -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
   ```
5. 配百度地图密钥（AK 不进代码库，只进本地文件，见【第三节】）：
   ```powershell
   Copy-Item .env.example .env
   # 用记事本打开 .env，把 BAIDU_AK / WEB_AK 换成自己的
   ```
6. 装前端依赖：`npm install`；
7. Web 形态直接就能跑：`npm run web`，浏览器打开 `http://127.0.0.1:8765`；
8. 桌面形态开发调试：`npm start`；
9. 要出成品才做【第四节】：生成图标 → 编译后端 → 打包 Electron；
10. 产物在 `release\`，按【第九节】自检过一遍。

> 只是开发调试或给评审演示，做到第 7 步就够，不需要 `logo.ico` 和 `server.exe`。
> **最快的验证途径是 Docker，见【第七节】。**

---

## 一、用到的工具

| 工具 | 版本 | 干什么用的 |
| --- | --- | --- |
| Node.js / npm | v24.9.0 / 11.9.0 | 跑 Electron 和打包命令行 |
| electron | 31.7.7 | 桌面外壳（只管开窗、进程守护、退出清理） |
| electron-builder | 26.15.3 | 出 NSIS 安装包 + 绿色版 zip |
| Python | 3.13（64 位） | 跑后端：FastAPI + httpx + 等时圈 / 盲区算法 |
| PyInstaller | 6.22.3 | 把 `server.py` 连同前端静态文件打成独立的 `server.exe` |
| png-to-ico | 3.0.2 | 把 PNG 转成标准的 Windows `.ico` |
| Docker Compose | 可选 | 给评委一键起 Web 版（免装 Python / Node） |

npm 源用国内镜像 `https://registry.npmmirror.com`；
Electron 与 electron-builder 的二进制同样从 npmmirror 下载（见【第四节】环境变量）。

安装依赖：

```powershell
npm config set registry https://registry.npmmirror.com
npm install --save-dev electron electron-builder png-to-ico
py -m pip install pyinstaller -i https://pypi.tuna.tsinghua.edu.cn/simple
```

Python 侧依赖写在 `requirements.txt`：`fastapi`、`uvicorn`、`httpx`、`pydantic`、
`python-dotenv`、`pytest`、`flake8`。

---

## 二、目录结构（为什么要这么切）

目录按「一个目录只负责一件事」来切，评审看代码结构时能直接对应功能点：

```text
life-circle-check\
├─ server.py              后端入口：起 FastAPI、挂载静态页面、注册路由
├─ app\
│  ├─ map_api.py          百度地图开放能力封装：地理编码 / POI 检索 / 批量距离矩阵
│  ├─ isochrone.py        等时圈算法：扇形采样 + 边界二分 + 空间插值
│  ├─ blindspot.py        服务盲区识别：网格采样 + 覆盖距离判定
│  ├─ cache.py            SQLite 缓存（同样的请求不重复烧 API 额度）
│  └─ config.py           读 .env 与用户配置，AK 统一出口
├─ web\                   纯静态前端（由后端托管，不依赖 Electron）
│  ├─ index.html
│  ├─ css\style.css
│  ├─ js\map.js  js\chart.js  js\api.js
│  └─ vendor\             本地化的 echarts 等第三方库（离线也能出图）
├─ main.js                Electron 主进程：拉后端、开窗口、退出清进程树
├─ preload.js             渲染进程安全桥
├─ assets\logo.png        图标源图
├─ logo.ico               生成的 Windows 图标
├─ requirements.txt       Python 依赖清单
├─ .env.example           环境变量样例（真 AK 不入库）
├─ scripts\build.bat      一键打包（把三步串起来）
├─ Dockerfile  docker-compose.yml      给评委的容器化跑法
├─ .gitee\workflows\build.yml          提交即自动构建
└─ release\               打包产物（不入库）
```

---

## 三、密钥脱敏：AK 的三种放法

本作品用到两类百度地图凭证，**都不能写死在代码里**：

| 凭证 | 用在哪 | 放哪 |
| --- | --- | --- |
| 浏览器端 AK（JS API） | 页面上的地图底图渲染 | 界面「设置」里填，或环境变量 `WEB_AK` |
| 服务端 AK（Web 服务 API） | 后端 POI 检索、路径规划、距离矩阵 | `.env` 的 `BAIDU_AK`，或用户目录的 `peizhi.json` |

读取优先级（`app/config.py` 的 `huoqu_ak()`，1 最高）：

1. 用户配置文件 `%APPDATA%\shenghuoquan\peizhi.json`（界面里填的，打包版走这条）；
2. 环境变量 `BAIDU_AK`（CI / Docker 走这条）；
3. 项目根 `.env`（本地开发走这条，已在 `.gitignore` 里）；
4. 都没有 → 后端返回 `{"code": 401, "msg": "未配置 AK"}`，
   页面弹出配置引导页，**不白屏、不把 AK 拼进日志**。

配套动作：

- `.env`、`.env.local`、`peizhi.json` 全部进 `.gitignore`；
- 日志里 AK 一律脱敏成 `BAIDU_AK=abc...xyz`（`app/config.py` 的 `yinshen()`）；
- 提交前自检，下面这条必须无结果：
  ```bash
  git grep -nE "(AK|ak|key|secret)[\"']?\s*[:=]\s*[\"'][A-Za-z0-9]{16,}"
  ```

---

## 四、打包步骤

### 第 1 步：生成图标 logo.ico

Windows 只认 `.ico`，jpg / png 都不行。先用 .NET 把源图缩成 256×256
（本机 Python 的 PIL 是坏的，`from PIL import Image` 会报
`cannot import name '_imaging'`，所以缩图走 .NET）：

```powershell
Add-Type -AssemblyName System.Drawing
$img = [System.Drawing.Image]::FromFile('assets\logo.png')
$bmp = New-Object System.Drawing.Bitmap 256, 256
$g = [System.Drawing.Graphics]::FromImage($bmp)
$g.InterpolationMode = [System.Drawing.Drawing2D.InterpolationMode]::HighQualityBicubic
$g.DrawImage($img, 0, 0, 256, 256)
$g.Dispose()
$bmp.Save('assets\logo-256.png', [System.Drawing.Imaging.ImageFormat]::Png)
$img.Dispose(); $bmp.Dispose()
```

再用 `png-to-ico` 转成 ico（别用 .NET 的 `Icon.Save()`，会报
`COM 组件调用返回 E_FAIL`）：

```js
const fs = require('fs');
const mod = require('png-to-ico');
const pngToIco = mod && mod.default ? mod.default : mod;   // 兼容 ESM 导出
pngToIco('assets/logo-256.png').then((buf) => fs.writeFileSync('logo.ico', buf));
```

生成的 ico 里应包含 16 / 32 / 48 / 256 四个尺寸。

### 第 2 步：打包 Python 后端成 server.exe

这一步会把 `web\` 整个静态目录一起打进去（`--add-data`），
所以桌面版不需要再单独放一份前端：

```powershell
py -m PyInstaller --noconfirm --clean --onefile --name server `
  --distpath build_pyi\dist --workpath build_pyi\work --specpath build_pyi `
  --add-data "web;web" `
  --hidden-import uvicorn.logging `
  --hidden-import uvicorn.loops.auto `
  --hidden-import uvicorn.protocols.http.auto `
  --hidden-import uvicorn.protocols.websockets.auto `
  --hidden-import email.mime.multipart `
  server.py

Copy-Item 'build_pyi\dist\server.exe' 'server.exe' -Force
Remove-Item -Recurse -Force 'build_pyi'
```

说明：

- 产物约 20~30MB（比纯后端大，是因为塞进了静态页面和依赖库）；
- `--onefile` 首启会在 `%TEMP%\_MEIxxxx` 解压，慢 1~3 秒属正常；
  嫌慢可换 `--onedir`，代价是多一个 `_internal` 文件夹要一起分发；
- **`server.py`、`app\`、`web\` 里任何一个文件改了都必须重做这一步**，
  否则桌面版跑的还是旧后端；
- uvicorn 那四个 hidden-import 缺一个，exe 起来就报
  `Error loading ASGI app`，务必带上。

### 第 3 步：打包 Electron 安装包

```powershell
# 1) 先把正在运行的程序关掉，否则文件被占用会打包失败
Get-Process server   -ErrorAction SilentlyContinue | Stop-Process -Force
Get-Process electron -ErrorAction SilentlyContinue | Stop-Process -Force

# 2) 关键环境变量
$env:NODE_OPTIONS = ''                       # 绕开 IDE 注入的 safe-delete 垫片
$env:ELECTRON_MIRROR = 'https://npmmirror.com/mirrors/electron/'
$env:ELECTRON_BUILDER_BINARIES_MIRROR = 'https://npmmirror.com/mirrors/electron-builder-binaries/'
$env:CSC_IDENTITY_AUTO_DISCOVERY = 'false'   # 没有代码签名证书，跳过签名探测

# 3) 开始打包（等价于 npm run dist）
npx electron-builder --win nsis zip

# 只要免安装的绿色目录（不出安装包）：
npx electron-builder --win --dir
```

---

## 五、package.json 里 build 字段的关键配置

```json
"scripts": {
  "start": "electron .",
  "web":   "python server.py",
  "dist":  "electron-builder --win nsis zip"
},
"build": {
  "appId": "com.xiaomuliang.shenghuoquan",
  "productName": "15分钟生活圈体检助手",
  "copyright": "Copyright © 2026 肖沐樑",
  "asar": true,
  "asarUnpack": ["**/*.exe", "**/*.node"],
  "directories": { "output": "release" },
  "files": [
    "main.js", "preload.js", "package.json",
    "server.exe", "logo.ico",
    "web/**", "assets/**",
    "!**/.env", "!**/.env.local", "!**/peizhi.json",
    "README.md", "LICENSE"
  ],
  "win": {
    "icon": "logo.ico",
    "target": [
      { "target": "nsis", "arch": ["x64"] },
      { "target": "zip",  "arch": ["x64"] }
    ]
  },
  "nsis": {
    "oneClick": false,
    "perMachine": false,
    "allowToChangeInstallationDirectory": true,
    "createDesktopShortcut": true,
    "createStartMenuShortcut": true,
    "shortcutName": "15分钟生活圈体检助手",
    "uninstallDisplayName": "15分钟生活圈体检助手 ${version}",
    "artifactName": "15分钟生活圈体检助手-安装版-${version}.exe"
  },
  "zip": {
    "artifactName": "15分钟生活圈体检助手-绿色版-${version}.zip"
  }
}
```

几条必须理解的：

- **`asar` 保持 `true`，靠 `asarUnpack` 放生 `server.exe`**：
  打包后路径会变成 `resources\app.asar.unpacked\server.exe`，
  主进程里必须这样取路径，否则 spawn 找不到文件：

  ```js
  const exe = path.join(__dirname, 'server.exe')
                  .replace('app.asar', 'app.asar.unpacked');
  ```

  不想折腾就把 `asar` 改成 `false`（上一个项目就是这么干的），
  代价是 `resources\app\` 下源码裸露能被直接拷走。
- **`files` 里显式排除 `.env`**：宁可让用户首启自己填 AK，
  也绝不能把密钥打进分发包。
- **`perMachine: false`**：装到 `%LOCALAPPDATA%\Programs\shenghuoquan`，
  不需要管理员权限。
- **`zip` 目标**：绿色版打成单个 zip，比散着几百个文件的 `win-unpacked\`
  好分发，评审下载解压即可用。

运行时后端的选用逻辑（`main.js` 的 `xuanzeHouduan()`）：

- 打包版：用包内 `server.exe`（用户机器免装 Python）；
- 开发版：跑源码 `server.py`，改完重启立刻生效；
- 都没有：弹明确提示，不静默失败。

数据存放位置（`main.js` 的 `zhunbeiShujuMulu()`）：

- 开发环境：项目里的 `data\`；
- 打包版：`%APPDATA%\shenghuoquan\`，下面放 `peizhi.json`（AK）
  和 `huancun.db`（API 缓存）—— 这样装到只读目录也能写，卸载重装缓存还在。

窗口加载方式：主窗口先 `loadURL('http://127.0.0.1:8765/')`，
启动后轮询 `/api/health`（最多 15 秒），通了再取消遮罩，避免白屏。

---

## 六、一键打包脚本 scripts\build.bat

评审要求「提供一键式编译/部署脚本」，所以把三步串成一个双击就跑的 bat：

```bat
@echo off
chcp 65001 >nul
setlocal

echo ========== 1/3 生成图标 ==========
if not exist logo.ico (
  powershell -ExecutionPolicy Bypass -File scripts\make-icon.ps1
)

echo ========== 2/3 编译后端 ==========
py -m PyInstaller --noconfirm --clean --onefile --name server ^
  --distpath build_pyi\dist --workpath build_pyi\work --specpath build_pyi ^
  --add-data "web;web" ^
  --hidden-import uvicorn.logging --hidden-import uvicorn.loops.auto ^
  --hidden-import uvicorn.protocols.http.auto ^
  --hidden-import uvicorn.protocols.websockets.auto server.py
if errorlevel 1 goto :fail
copy /Y build_pyi\dist\server.exe server.exe >nul
rmdir /S /Q build_pyi 2>nul
del /Q server.spec 2>nul

echo ========== 3/3 打包桌面版 ==========
taskkill /F /IM server.exe /T 2>nul
taskkill /F /IM electron.exe /T 2>nul
set NODE_OPTIONS=
set CSC_IDENTITY_AUTO_DISCOVERY=false
set ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/
set ELECTRON_BUILDER_BINARIES_MIRROR=https://npmmirror.com/mirrors/electron-builder-binaries/
call npx electron-builder --win nsis zip
if errorlevel 1 goto :fail

echo ========== 完成，产物在 release\ ==========
dir release\*.exe release\*.zip
pause
exit /b 0

:fail
echo !!! 打包失败，看上面的报错
pause
exit /b 1
```

配套的 `scripts\make-icon.ps1` 就是【第 1 步】那两段脚本合起来，作用同上。

---

## 七、给评委的 Docker 跑法（强烈建议配）

Web 形态容器化后，评委不需要装 Python / Node，两条命令就能验证：

```yaml
# docker-compose.yml
services:
  app:
    build: .
    ports: ["8765:8765"]
    environment:
      BAIDU_AK: ${BAIDU_AK}          # 评委填自己的，不写死进镜像
      WEB_AK:   ${WEB_AK}
    restart: unless-stopped
```

```dockerfile
FROM python:3.13-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
COPY . .
EXPOSE 8765
CMD ["python", "server.py"]
```

评委用法：

```bash
cp .env.example .env        # 填入自己的 AK
docker compose up -d
# 浏览器打开 http://localhost:8765
```

---

## 八、CI/CD（Gitee Go / GitHub Actions）

`.gitee\workflows\build.yml`，每次 push 自动做「代码规范检查 + 算法单测 + 打包」：

```yaml
name: build
on: [push, pull_request]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.13' }
      - run: pip install -r requirements.txt flake8
      - run: flake8 app server.py --max-line-length=120      # 代码规范门禁
      - run: python -m pytest tests -q                       # 算法单测（等时圈 / 盲区）
  pack:
    needs: check
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20', registry-url: 'https://registry.npmmirror.com' }
      - run: npm install
      - run: npx electron-builder --win nsis zip
        env: { CSC_IDENTITY_AUTO_DISCOVERY: 'false' }
      - uses: actions/upload-artifact@v4
        with: { name: release, path: release/*.exe }
```

AK 一律走仓库 Secrets，日志里不打印。

---

## 九、踩过的坑（都别再踩）

1. **窗口图标不显示**：`new BrowserWindow({ icon })` 里写了 png，
   Windows 只认 `.ico`。→ 改 `logo.ico`，并加
   `app.setAppUserModelId('15分钟生活圈体检助手')`，否则任务栏图标分组不对。
2. **PIL 坏了**：本机 `from PIL import Image` 报 `cannot import name '_imaging'`。
   → 缩图改用 .NET `System.Drawing`。
3. **`.NET Icon.Save()` 存 ico 报 COM E_FAIL**。→ 用 `png-to-ico`。
4. **打包时报 `[safe-delete] 操作失败 ... trash operation`**：
   编辑器给 node 注入了 safe-delete 垫片，中文路径下删除失败。
   → 打包前 `$env:NODE_OPTIONS = ''`，删除目录改用 `fs.rmSync(..., { force: true })`。
5. **`release` 删不掉 / 覆盖安装失败**：旧版本进程还在跑。
   → 打包前 `taskkill /F /IM server.exe /T`、`/IM electron.exe /T`。
6. **关窗后后端残留、占着 8765**：PyInstaller 单文件 exe 会派生子进程，
   只 kill 父进程会留孤儿进程。→ `killServer()` 用 `taskkill /F /T /PID`
   结束整棵进程树，并在启动前 `shifangDuankou()` 兜底清端口。
7. **`server.exe` 起来就闪退，报 `Error loading ASGI app`**：
   uvicorn 的 hidden-import 漏了。→ 补上 `--hidden-import uvicorn.*`。
8. **exe 里读不到 `web\index.html`**：没用 `--add-data`，
   或代码里用相对路径取静态目录。→ 用 `--add-data "web;web"`，
   代码里按 `sys._MEIPASS` 判断（打包后资源在临时解压目录）：

   ```python
   import sys, pathlib
   _base = pathlib.Path(getattr(sys, '_MEIPASS', pathlib.Path(__file__).parent))
   app.mount('/', StaticFiles(directory=str(_base / 'web'), html=True), name='web')
   ```
9. **asar 里 spawn exe 找不到文件**：没配 `asarUnpack`，
   或没把 `app.asar` 替换成 `app.asar.unpacked`。→ 见【第五节】。
10. **地图白屏 / 控制台报 AK 校验失败**：配置里塞的是服务端 AK，
   JS API 要用浏览器端 AK，两者不能混用。→ 检查【第三节】两类凭证。
11. **评审机器上查询很慢**：等时圈采样点太多触发 QPS 限流。
   → 并发控制在 4 以内，结果进 SQLite 缓存，同样的样例社区第二次秒开。
12. **改了 `server.py` / `web\` 但桌面版没变化**：忘了重做【第 2 步】。

---

## 十、产物与分发

打包完成后 `release\` 里是：

| 文件 | 大小 | 说明 |
| --- | --- | --- |
| `15分钟生活圈体检助手-安装版-1.0.0.exe` | 约 100MB | 向导式安装包，双击安装 |
| `15分钟生活圈体检助手-绿色版-1.0.0.zip` | 约 100MB | 解压即用，不用安装、不用管理员权限 |
| `win-unpacked\` | 约 300MB | 中间产物，调试用，一般不分发 |

- 安装包会创建**桌面快捷方式**和**开始菜单项**，图标就是 `logo.ico`，带卸载程序；
- 两种桌面形态都**不需要目标机器装 Python 和 Node**，
  但都需要**联网**（要去百度地图开放平台取数据），这点要在 README 里写清楚；
- 用户配置与缓存在 `%APPDATA%\shenghuoquan\`，卸载不会被删，重装直接可用；
- 重新发布了要**覆盖安装**（走同一个安装路径），旧版本才会被替换。

---

## 十一、一句话重打包流程

```bat
scripts\build.bat
```

想手动分步也能跑：

```powershell
# 1. server.py / app\ / web\ 有改动时才做：重新生成后端
py -m PyInstaller --noconfirm --clean --onefile --name server `
  --distpath build_pyi\dist --workpath build_pyi\work --specpath build_pyi `
  --add-data "web;web" --hidden-import uvicorn.logging server.py
Copy-Item 'build_pyi\dist\server.exe' 'server.exe' -Force; Remove-Item -Recurse -Force build_pyi

# 2. 关掉正在运行的程序，然后出安装包
Get-Process server,electron -ErrorAction SilentlyContinue | Stop-Process -Force
$env:NODE_OPTIONS = ''; npm run dist
```

产物在 `release\15分钟生活圈体检助手-安装版-1.0.0.exe`。

---

## 十二、编译后自检（每次出包都过一遍）

1. `release\15分钟生活圈体检助手-安装版-1.0.0.exe` 和
   `release\15分钟生活圈体检助手-绿色版-1.0.0.zip` 都已生成；
2. 解压绿色版，双击 `15分钟生活圈体检助手.exe`，程序能正常打开；
3. 浏览器访问 `http://127.0.0.1:8765/api/health` 返回 `{"ok": true, "port": 8765}`；
4. 任务管理器里**没有 python 进程**，只有 `server.exe` ——
   说明用的是内置后端，"目标机器免装 Python"这一条成立；
5. 在页面上输入一个社区中心点，能渲染出**等时圈热力图**，并列出圈内
   菜市场 / 药店 / 小学等 POI 覆盖情况与「服务盲区」；
6. 关掉程序后再看一次：`server.exe` 已退出、8765 端口被释放
   （`netstat -ano | findstr LISTENING | findstr :8765` 无输出）。

---

## 十三、编译报错速查

| 报错 / 现象 | 原因 | 处理 |
| --- | --- | --- |
| `[safe-delete] 操作失败 ... trash operation` | 编辑器给 node 注入了 safe-delete 垫片 | 打包前 `$env:NODE_OPTIONS = ''` |
| 打包时提示文件被占用、`release` 删不掉 | 旧程序还在运行 | 结束 `server`、`electron` 进程以及占用 8765 的进程 |
| 下载 electron 一直卡住或超时 | 国外源太慢 | 设 `ELECTRON_MIRROR`、`ELECTRON_BUILDER_BINARIES_MIRROR` 指向 npmmirror |
| 报 signing / signtool 相关错误 | 没有代码签名证书 | 设 `CSC_IDENTITY_AUTO_DISCOVERY=false` |
| `electron-builder: command not found` | 打包依赖没装 | `npm install --save-dev electron-builder` |
| 装完打开白屏 / 连不上后端 | 后端缺失或端口被占 | 确认 `server.exe` 在；按【坑 6】清掉残留进程 |
| 窗口 / 任务栏图标还是 Electron 的 | 图标不是 `.ico`，或是开发模式 | 用 `logo.ico`；`npm start` 时任务栏图标固定是 Electron，属正常 |
| `Error loading ASGI app` | uvicorn hidden-import 漏了 | 补 `--hidden-import uvicorn.*` 后重打 |
| 改了后端但装出来还是旧行为 | 没重新编译后端 | 重做【第 2 步】生成新的 `server.exe` |

---

## 附、交付物对照评分点（自检用）

| 评审维度 | 对应本文做法 |
| --- | --- |
| 开源应用源码 + 许可证 | 全仓托管 Gitee/GitHub，附 `LICENSE`（Apache 2.0 / MIT） |
| 一键式编译/部署脚本 | 【第六节】`scripts\build.bat` + 【第七节】`docker-compose.yml` |
| 技术设计文档 | 另见《技术设计报告.md》：等时圈算法、多源 POI 清洗、盲区识别、API 调用策略 |
| 功能正确性（40%） | 地理编码 / POI / 路径规划真实调用；地图渲染等时圈；盲区识别 |
| API 深度与工程优化（30%） | 批量距离矩阵 + 并发 ≤4 + SQLite 缓存；AK 缺失 / QPS 限流的兜底 |
| 产品交互（15%） | echarts 热力图 / 柱状图 / 各维度评分进度条（星级）；自定义中心点 |
| 开源工程规范（15%） | 模块清晰；`README` 含 AK 脱敏配置说明；【第八节】CI 门禁 |
