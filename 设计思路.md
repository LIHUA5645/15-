# 15 分钟生活圈智能体检助手 · 产品设计思路

作者：肖沐樑　QQ：3387432690
完成时间：2026，09，18

本文是赛题作品的**产品设计思路草案**，回答三个问题：
这个应用是什么、用户怎么用、底层怎么算。编译打包相关见《方法.md》。

---

## 一、产品定位

一句话：给**社区规划者 / 街道办 / 普通居民**用的「社区配套体检工具」。
用户丢一个社区中心点进去，系统自动算出**真实步行 15 分钟能到的范围**（不是直线圆），
盘点圈内菜场 / 药店 / 小学 / 社区医院等配套够不够，标出「设施荒漠」，最后吐一份带分数的体检报告。

价值主张：把"服务盲区"从拍脑袋变成可量化、可可视化、可复盘的硬指标。

---

## 二、目标用户与使用场景

| 角色 | 典型场景 |
| --- | --- |
| 街道 / 社区规划人员 | 选定一个街道，体检现有配套，找出要补建菜场 / 卫生站的位置 |
| 参赛评审 | 打开即用，输入样例社区，看等时圈渲染、POI 统计、盲区清单 |
| 普通居民 | 输入自己小区，看 15 分钟能不能走到菜场 / 药店 / 小学 |

---

## 三、用户交互流程

1. 打开应用 → 地图选点（点地图 / 搜地址 / 直接输经纬度），选样例城市与社区；
2. 勾选要体检的设施类型：菜市场、药店、小学、社区医院、公园、银行等；
3. 设置步行时长（默认 15 分钟，可改 5/10/15/20）；
4. 点「开始体检」→ 进度条（后台在攒百度地图 API）；
5. 出结果页（见第四节）；
6. 可导出报告（图片 / 可复制表格 / PDF）。

---

## 四、界面布局（草案）

```text
┌───────────────────────────────────────────────────────────┐
│  15 分钟生活圈智能体检助手        [城市▾] [社区搜索框🔍]     │
├──────────────────────────────────────┬────────────────────┤
│                                      │  体检报告           │
│      百度地图底图                     │  ┌──────────────┐  │
│   · 等时圈热力图层（时间场）          │  │ 综合得分 ★★★☆│  │
│   · POI 标记（按类型着色）            │  └──────────────┘  │
│   · 灰色盲区高亮                      │  各维度评分：        │
│   · 中心点标注                        │   菜市场  █████★ 5 │
│                                      │   药店    ███☆☆ 3  │
│   [设施类型多选] [时长▾] [开始体检]   │   小学    ██☆☆☆ 2  │
│                                      │   社区医院███☆☆ 3  │
│                                      │  ───────────────  │
│                                      │  柱状图：各类数量   │
│                                      │  盲区清单：          │
│                                      │   · 西南角 1km 无菜场│
│                                      │   · 东北角 1km 无小学│
└──────────────────────────────────────┴────────────────────┘
```

左侧地图是主舞台，右侧报告区用**评分星级 + 进度条**和**柱状图**呈现，没有雷达图。

---

## 五、核心算法设计

### 5.1 等时圈生成（不拿路网原始数据）

赛题鼓励"在不获取底层路网数据的前提下，通过对周边分散点位的 API 测时结果
进行空间插值或扇形采样，合理推导出近似连通区域"。采用「扇形采样 + 批量距离矩阵 + 空间插值」：

1. **扇形射线采样**：以中心点出发，沿 24~36 个方向均匀发射射线；
2. **候选点布设**：每个方向上按递增半径（如 200m / 400m / … / 1500m）布候选点，
   形成「方向 × 半径」的候选点阵；
3. **批量测时**：以中心点为唯一起点，对全部候选点调用百度**批量距离矩阵 API**
   （一次请求拿到起点 × 多终点的步行时长矩阵），得到每个候选点的「步行可达分钟数」；
4. **空间插值**：用 IDW（反距离加权）把离散候选点的时间值插值为连续「时间场」栅格；
5. **取等值线**：时间场 ≤ 设定时长（默认 15min）的区域即等时圈，渲染成热力图层。

> 注：若单次矩阵点数受限，按「方向分批」切片并发请求，见第六节。

### 5.2 服务盲区识别

1. 在等时圈范围内撒网格（如 50m 格）；
2. 对每个网格点，用 POI 检索 + 距离矩阵判定「1km 内是否存在该类设施」；
3. 全覆盖不到的连续网格区域标记为「灰色盲区」；
4. 输出盲区清单（方位 / 缺失设施类型 / 影响范围）。

### 5.3 覆盖度评分

每类设施按两条规则打分（1~5 星）：

- 圈内该类设施数量是否达到「宜居基线」（如菜场 ≥1、小学 ≥1、药店 ≥1）；
- 最近设施的可达时长（越短分越高）；

加权汇总成综合得分，短板维度一眼可见，对应右侧星级进度条。

---

## 六、工程优化（冲 API 深度 30% 那档）

| 手段 | 做法 |
| --- | --- |
| 批量距离矩阵 | 用矩阵 API 取代逐点路线规划，请求数降一个量级 |
| 并发控制 | 并发 ≤ 4，超过则排队；避免触发百度 QPS 限流 |
| SQLite 缓存 | key = (中心经纬度四舍五入 + 半径 + 设施类型 + 步行方式)，命中即秒出，不重复烧额度 |
| 容错降级 | API 超时 / 限流 / 返回异常时，用 Haversine 直线距离 ÷ 步行速度（≈1.2 m/s）兜底估算，绝不白屏 |
| 退避重试 | 429 / 5xx 走指数退避，最多 3 次 |

---

## 七、可视化方案

- **等时圈热力图**：时间场栅格按分钟着色，叠在百度底图上；
- **POI 标记**：按设施类型着色聚合；
- **柱状图**：圈内各类设施数量对比；
- **评分星级 / 进度条**：各维度覆盖度评分（菜场 / 药店 / 小学 / 社区医院 / 公园 / 银行），短板维度突出显示；
- **灰度盲区高亮** + 文字清单。

> 明确：**不使用雷达图**，覆盖度评分用星级 + 进度条表达。

---

## 八、技术栈与目录结构

后端：Python 3.13 + FastAPI + httpx + pydantic + python-dotenv + SQLite
前端：原生 HTML/CSS/JS + 百度地图 JS API + ECharts（本地化到 `web/vendor`）
桌面壳：Electron 31 + electron-builder
打包：PyInstaller（后端）→ server.exe；electron-builder（壳）→ 安装包 / 绿色 zip

```text
life-circle-check\
├─ server.py              后端入口：FastAPI + 静态托管 + 路由
├─ app\
│  ├─ map_api.py          百度地图封装：地理编码 / POI 检索 / 批量距离矩阵
│  ├─ isochrone.py        等时圈：扇形采样 + 批量测时 + IDW 插值
│  ├─ blindspot.py        服务盲区：网格采样 + 覆盖判定
│  ├─ score.py            覆盖度评分（星级）
│  ├─ cache.py            SQLite 缓存
│  └─ config.py           AK / 配置读取（脱敏）
├─ web\                   纯静态前端（后端托管）
│  ├─ index.html
│  ├─ css\style.css
│  ├─ js\map.js  js\chart.js  js\api.js  js\score.js
│  └─ vendor\             echarts 等本地库
├─ main.js / preload.js   Electron 壳
└─ ...（见《方法.md》）
```

> 注意：`score.py` 与 `js/score.js` 是新增模块，对应评分逻辑，方法.md 目录树可同步补上。

---

## 九、双形态架构

后端 `FastAPI` 同时托管静态页面与算法 API，因此：

- **Web 形态**：浏览器直连 `http://127.0.0.1:8765`，评审 / Docker 直接验；
- **桌面形态**：Electron 壳只负责拉起 `server.exe`、开窗口、退出生死清理，业务逻辑全在后端。

两种形态共用一份代码，避免界面改一处同步两处。

---

## 十、百度地图 API 调用策略

| 能力 | 接口 | 用途 |
| --- | --- | --- |
| 地理编码 / 逆地理编码 | Geocoding API | 地址↔坐标互转，支撑"搜社区" |
| POI 检索 | Place API（周边检索） | 拿圈内设施点 |
| 批量距离矩阵 | RouteMatrix（轻量算路） | 中心 × 多候选点步行时长，等时圈核心 |
| 路线规划 | Direction API | 单点详查 / 降级校验 |

调用纪律：所有密钥走环境变量 / 用户配置，不入库；日志脱敏；异常统一兜底。

---

## 十一、评审维度逐条对照

| 评审维度 | 本设计的对应做法 |
| --- | --- |
| 功能正确性 40% | 真实调地理编码 / POI / 路径规划；地图渲等时圈；盲区识别 |
| API 深度 30% | 批量矩阵 + 并发 ≤4 + SQLite 缓存 + 容错降级 + 扇形采样插值 |
| 产品交互 15% | 热力图 / 柱状图 / 评分星级进度条；自定义中心点；报告导出 |
| 开源规范 15% | 模块清晰；《方法.md》含 AK 脱敏配置；CI 规范门禁（见方法.md 第八节） |

---

## 十二、待确认与后续步骤

1. 样例城市 / 社区定哪个（影响演示数据）；
2. 设施类型清单与"宜居基线"数量（评分阈值）由谁拍板；
3. 是否要报告导出（PDF / 图片 / 表格），导出哪些字段；
4. 下一步建议优先级：
   - 先打通后端 `app/map_api.py` + `isochrone.py`，用浏览器 / Postman 验等时圈；
   - 再补 `blindspot.py` + `score.py`；
   - 最后做前端可视化与 Electron 壳；
   - 交付物（build.bat / Docker / CI）按《方法.md》同步建。
